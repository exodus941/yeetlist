<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The site's own page colour, so the window behind the page and the
         strip under the status bar never show a different shade while the
         page loads. -->
    <color name="page">#0F0F0F</color>
    <!-- The launcher icon ground, the stylesheet own --accent. -->
    <color name="accent">#FF0044</color>
</resources>
